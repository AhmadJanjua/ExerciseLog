services:
  dev:
    container_name: exercise-dev
    image: exercise-dev-img
    user: "${UID}:${GID}"
    working_dir: /app
    volumes:
      - ./exercise-app:/app
    ports:
      - "5173:5173"
      - "5174:5174"
    command: ["bash", "-lc", "npm install && npm run dev -- --host 0.0.0.0"]